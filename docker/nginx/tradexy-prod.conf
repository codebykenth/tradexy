# tradexy.site → Production (Docker port 8082)
server {
    listen 80;
    server_name tradexy.site www.tradexy.site;

    # Redirect to HTTPS (Certbot will add this automatically)
    # For initial setup, this serves HTTP until SSL is configured

    location / {
        proxy_pass http://127.0.0.1:8082;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket support (if needed)
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Max upload size (match Laravel's upload limit)
    client_max_body_size 64M;
}
